# Limitations — sanitized public package

- Full operational evidence is private only; public branch contains aggregates and hashes.
- No search terms in text open are versioned.
- No row-level keywords/search terms/API-call lists are versioned.
- Account suspension state is proven, but the literal suspension reason is not exposed by API.
- Some Google intelligence collection/projection already exists in VOLC; this mission does not prove the complete continuous alert delivery and operator reaction loop.
- Destination host roles were not measured distinctly in the original evidence; public facts therefore mark clickable/form/script/resource/redirect separations as `not_measured` where appropriate.
- No new Google Ads, Supabase, Data Manager or live page calls were made during this sanitization microcorrection.
